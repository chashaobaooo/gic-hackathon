import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { useState } from "react"; // ❌ unused

export default function Page() {
  return <div className="text-red-500">Hello</div>; // ❌ should be single quotes
}